[package]
name = "superface_core"
version = "0.1.0"
edition = "2021"

[lib]
crate-type = ["cdylib"]

[dependencies]
anyhow = "1"
thiserror = "1"

wasmi = "0.25"
quickjs-wasm-rs = { version = "0.1", features = [] }

serde = { version = "1", features = ["derive"] }
serde_json = { version = "1"}

slab = "0.4"
