[package]
name = "core"
version = "0.1.0"
edition = "2021"

[dependencies]
anyhow = "1"
thiserror = "1"

wasmi = "0.25"

serde = { version = "1", features = ["derive"] }
serde_json = { version = "1"}
