{
  "name": "cloudflare_worker",
  "version": "0.1.0",
  "main": "dist/index.js",
  "license": "MIT",
  "private": true,
  "dependencies": {
    "@cloudflare/workers-wasi": "^0.0.5",
    "@superfaceai/one-sdk-cloudflare": "../../host/js/cloudflare"
  },
  "devDependencies": {
    "typescript": "^5.0.2",
    "wrangler": "^2.16.0"
  },
  "scripts": {
    "dev": "wrangler dev --local",
    "publish": "wrangler publish"
  }
}