{
  "name": "cloudflare_worker",
  "version": "0.1.0",
  "main": "dist/index.js",
  "license": "MIT",
  "private": true,
  "dependencies": {
    "@superfaceai/one-sdk": "file:../../host/js"
  },
  "devDependencies": {
    "typescript": "^5.0.2",
    "wrangler": "^2.18.0"
  },
  "scripts": {
    "dev": "yarn install --force && wrangler dev --local",
    "publish": "wrangler publish",
    "build:debug": "rm -rf dist && yarn install --force && wrangler publish --dry-run --outdir=dist"
  }
}
