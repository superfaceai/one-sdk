{
  "name": "@superfaceai/map-std",
  "version": "0.1.0",
  "module": "src/std.ts",
  "main": "dist/map_std.js",
  "types": "types/map_std",
  "license": "MIT",
  "devDependencies": {
    "@rollup/plugin-typescript": "^11.1.0",
    "@types/core-ffi": "*",
    "rimraf": "^4.4.1",
    "rollup": "^3.21.3",
    "rollup-plugin-dts": "^5.3.0",
    "tslib": "^2.5.0",
    "typescript": "^5.0.2"
  },
  "scripts": {
    "check": "tsc -p tsconfig.json --noEmit",
    "build:std": "tsc -p tsconfig.json",
    "build:map_std": "rollup --config --bundleConfigAsCjs",
    "build": "yarn check && rimraf types/ dist/ && yarn build:map_std"
  }
}