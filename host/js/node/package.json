{
  "name": "@superfaceai/one-sdk-node",
  "version": "0.1.0",
  "main": "dist/index.js",
  "license": "MIT",
  "devDependencies": {
    "@types/node": "^18.15.11",
    "@superfaceai/one-sdk-common": "*"
  },
  "scripts": {
    "build:types": "rimraf types && tsc -p tsconfig.json --emitDeclarationOnly",
    "build": "yarn build:types && rimraf dist && esbuild src/index.ts --bundle --platform=node --loader:.wasm=file --outdir=dist"
  }
}