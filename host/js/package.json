{
  "name": "@superfaceai/one-sdk",
  "version": "3.0.0-alpha.6",
  "main": "node/index.js",
  "type": "module",
  "license": "MIT",
  "devDependencies": {
    "@types/node": "^18.15.11",
    "rimraf": "^5.0.0",
    "typescript": "^5.0.2"
  },
  "dependencies": {
    "@cloudflare/workers-wasi": "^0.0.5"
  },
  "scripts": {
    "check": "tsc -p tsconfig.json --noEmit",
    "clean": "rimraf common cloudflare node",
    "build": "yarn clean && tsc -p tsconfig.json",
    "test": "yarn run check"
  },
  "files": [
    "assets/**/*",
    "common/**/*",
    "node/**/*",
    "cloudflare/**/*"
  ]
}