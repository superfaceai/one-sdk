{
  "name": "@superfaceai/one-sdk-cloudflare",
  "version": "0.1.0",
  "main": "dist/index.js",
  "types": "types/index",
  "license": "MIT",
  "devDependencies": {
    "@cloudflare/workers-wasi": "^0.0.5",
    "@superfaceai/one-sdk-common": "*"
  },
  "scripts": {
    "build:types": "rimraf types && tsc -p tsconfig.json --emitDeclarationOnly",
    "build": "yarn build:types && rimraf dist && esbuild src/index.ts --bundle --loader:.wasm=copy --outdir=dist --format=esm"
  }
}
